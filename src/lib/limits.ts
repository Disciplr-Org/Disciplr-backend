export const LIMITS = {\n  PAGINATION_DEFAULT_LIMIT: 25,\n  PAGINATION_MAX_LIMIT: 100,\n  PAGINATION_MAX_PAGE: 1000,\n  CHART_MAX_POINTS: 500,\n  CHART_MAX_SERIES: 10,\n  UPLOAD_MAX_BYTES: 10 * 1024 * 1024, // 10 MB\n  RATE_LIMIT_WINDOW_MS: 60 * 1000,\n  RATE_LIMIT_MAX_REQUESTS: 300,\n  CONCUTRENT_REQUESTS_PER_ORG: 20,\n  POLLING_MIN_INTERVAL_MS: 5000,\n} as const;\n\nexport function clampInt(value: unknown, defaultValue: number, max: number): number {\n  const n = typeof value === "string" ? Number.parseInt(value, 10) : Number(value);\n  if (!Number.isFinite(n) || n <= 0) return defaultValue;\n  return Math.min(n, max);\n}\n\nexport function assertUploadSize(length: number): void {\n  if (length > LIMITS.UPLOAD_MAX_BYTES) {\n    const err: any = new Error(`Upload exceeds maximum size of ${LIMITS.UPLOAD_MAX_BYTES} bytes`);\n    err.status = 413;\n    throw err;\n  }\n}\n