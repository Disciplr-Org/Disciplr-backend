import { LIMITS, clampInt } from "./limits.js";\n\nexport interface Pagination {\n  page: number;\n  limit: number;\n  offset: number;\n}\n\nexport function getPagination(query: Record<string, unknown>): Pagination {\n  const page = clampInt(query.page, 1, LIMITS.PAGINATION_MAX_PAGE);\n  const limit = clampInt(query.limit, LIMITS.PAGINATION_DEFAULT_LIMIT, LIMITS.PAGINATION_MAX_LIMIT);\n  return { page, limit, offset: (page - 1) * limit };\n}\n